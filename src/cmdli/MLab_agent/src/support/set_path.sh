# Set the path variables to hit the MIC directories
#

PATH="${PATH};${SIMSERVER_DIR}/bin/${TARGET_OS}"
PATH="${PATH};${OPCON_DIR}/bin/${TARGET_OS}"
#PATH="${PATH};${MIC_RPCGEN_DIR}/bin/${TARGET_OS}"
PATH="${PATH};${MIC_RPCGEN_DIR}/bin/linux"
PATH="${PATH};${SUPPORT_DIR}"
